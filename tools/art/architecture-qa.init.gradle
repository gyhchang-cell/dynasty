// Isolated disposable GameTest world; never uses the launcher's saves or the normal run directory.
gradle.projectsEvaluated {
    rootProject.minecraft.runs.gameTestServer {
        workingDirectory rootProject.file('build/architecture-gametest')
    }
}
